<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>MCP Authorization - {{ project_name }}</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.3);
            max-width: 420px;
            width: 100%;
            padding: 40px;
        }
        .logo {
            text-align: center;
            margin-bottom: 30px;
        }
        .logo h1 {
            font-size: 24px;
            color: #1a1a2e;
        }
        .logo p {
            color: #666;
            margin-top: 8px;
        }
        h2 {
            font-size: 20px;
            color: #333;
            margin-bottom: 20px;
            text-align: center;
        }
        .permissions {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 16px;
            margin-bottom: 24px;
        }
        .permissions h3 {
            font-size: 14px;
            color: #666;
            margin-bottom: 12px;
        }
        .permissions ul {
            list-style: none;
        }
        .permissions li {
            padding: 8px 0;
            color: #333;
            display: flex;
            align-items: center;
        }
        .permissions li::before {
            content: "✓";
            color: #28a745;
            margin-right: 10px;
            font-weight: bold;
        }
        .error {
            background: #f8d7da;
            color: #721c24;
            padding: 12px;
            border-radius: 6px;
            margin-bottom: 20px;
            text-align: center;
        }
        label {
            display: block;
            font-size: 14px;
            color: #666;
            margin-bottom: 8px;
        }
        input[type="password"] {
            width: 100%;
            padding: 12px 16px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 16px;
            margin-bottom: 24px;
            transition: border-color 0.2s;
        }
        input[type="password"]:focus {
            outline: none;
            border-color: #007bff;
        }
        .buttons {
            display: flex;
            gap: 12px;
        }
        button {
            flex: 1;
            padding: 14px;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.1s, box-shadow 0.2s;
        }
        button:hover {
            transform: translateY(-1px);
        }
        button:active {
            transform: translateY(0);
        }
        .btn-allow {
            background: #007bff;
            color: white;
            box-shadow: 0 4px 12px rgba(0,123,255,0.3);
        }
        .btn-allow:hover {
            background: #0056b3;
        }
        .btn-deny {
            background: #6c757d;
            color: white;
        }
        .btn-deny:hover {
            background: #545b62;
        }
        .client-info {
            font-size: 12px;
            color: #999;
            text-align: center;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="logo">
            <h1>{{ project_name }}</h1>
            <p>MCP Authorization</p>
        </div>

        <h2>{{ client_id }} requests access</h2>

        <div class="permissions">
            <h3>Requested permissions:</h3>
            <ul>
                <li>Access MCP tools and resources</li>
                <li>Manage Sulu CMS content</li>
                <li>Execute page operations</li>
            </ul>
        </div>

        {% if error %}
        <div class="error">{{ error }}</div>
        {% endif %}

        <form method="post">
            <label for="password">Project Password</label>
            <input type="password" id="password" name="password" placeholder="Enter project password" required autofocus>

            <div class="buttons">
                <button type="submit" name="action" value="deny" class="btn-deny">Deny</button>
                <button type="submit" name="action" value="allow" class="btn-allow">Allow</button>
            </div>
        </form>

        <p class="client-info">
            Redirect: {{ redirect_uri|slice(0, 50) }}{% if redirect_uri|length > 50 %}...{% endif %}
        </p>
    </div>
</body>
</html>
