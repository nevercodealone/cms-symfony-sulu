{% extends "base.html.twig" %}

{% block content %}
    {% for block in content.blocks %}
        {% include "includes/ethic/" ~ block.type ~ ".html.twig" with {content: block}%}
    {% endfor %}

    <section class="tz-lastest-event tz-lastest-event-style-4">
        <h3 class="tz-event-title">{{ content.eventCarouselHeadline }}</h3>
        <div class="container">
            <div class="tz-event-container">
                <div class="tz-event-wrapper owl-carousel owl-theme">
                    {% for eventCarouselItem in content.eventCarouselItems %}
                        <div class="tz-event-style-4">
                            <div class="tz-image-event-item">
                                <img src="{{ eventCarouselItem.excerptImages[0].url }}" alt="{{ eventCarouselItem.title }}">
                                <a href="https://youtu.be/4Z_JgTUYKsc" target="_blank" rel="noopener"><i class="fa fa-arrows-alt"></i></a>
                            </div>
                            <div class="tz-content-event-item">
                                <h6>{{ eventCarouselItem.title }}</h6>
                                <strong>{{ eventCarouselItem.date|localizeddate('long', 'none', "de") }} @ 9:00 am</strong>
                                <img src="/ethic/images/check-01-home1.png" style="display: unset; width: unset" alt="{{ eventCarouselItem.title }}">
                                <p>
                                    <strong><a href="{{ eventCarouselItem.blogLink }}" target="_blank" rel="noopener">{{ eventCarouselItem.blogLinkText }}</a></strong>
                                    {{ eventCarouselItem.description }}
                                    <br>
                                    <strong>
                                        Spende {{ eventCarouselItem.donation }},- Euro
                                        <br>
                                        <a href="{{ eventCarouselItem.socialInstitutionLink }}" target="_blank" rel="noopener">
                                            {{ eventCarouselItem.socialInstitutionLinkText }}
                                        </a>
                                    </strong>
                                </p>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </section><!-- End class tz-lastest-event theme-white -->
    <section class="tz-introduce-style-4" data-q="form-contact">
        <div class="container">
            <div class="tz-address-style-4">
            <span>
                <img src="{{ content.introduce_image[0].url }}"
                     alt="Duisburg Never Code Alone">
            </span>
                <ul class="tz-phone-fax">
                    <li><a href="#"><img src="{{ content.introduce_logo[0].url }}" alt="Never Code Alone Logo"></a></li>
                    <li>
                        <img src="/ethic/images/check-home4.png" alt="Adresse">
                        <span class="theme-color-white">{{ content.introduce_address }}</span>
                    </li>
                    <li>
                        <img src="/ethic/images/check-home4.png" alt="Telefon">
                        <span class="theme-color-white">{{ content.introduce_phone }}</span>
                    </li>
                </ul>
            </div>
            <div class="tz-introduce-content tz-introduce-content-style-4">
                <h4>{{ content.introduce_headline }}</h4>
                <p>
                    {{ content.introduce_text|raw }}
                </p>
            </div>
        </div><!-- End container class tz-home-4-introduce -->
    </section><!-- End class tz-home-4-introduce -->
    {{ dump() }}
{% endblock %}
